# Ordering does not matter in this file! 

# Euclidian dimensions
dim 3

# size in each direction (preferably even numbers)
L1 10
L2 10
L3 30

# iterations
iterations 1000000

# how often to write measurements
interval 10

# how often to write lattice configuration to file 
checkpoint 1000

# perform initial sanity checks on lattice layout?
run_checks 1

# where results are written
resultsfile measure

# parameters

# gauge couplings
betasu2 8.0

# doublet
msq -0.207404
lambda 0.00915047

# triplet
msq_triplet 0.2
b4 0.2
a2 0.1

# initial values for fields
phi0 0.2
sigma0 0.2 


### update algorithms

# SU(2) links: metropolis or heatbath
algorithm_su2link heatbath
# SU(2) doublets: metropolis or overrelax
algorithm_su2doublet overrelax
# SU(2) triplet: metropolis or overrelax
algorithm_su2triplet overrelax

# where to store the lattice at the end
latticefile lattice

# how many times per sweep to update the two fields
update_doublet 1
update_triplet 1

### multicanonical
multicanonical 1
bins 80
min 0.505
max 3.1
# where to store the weight?
weightfile weight
# set infinite weight outside the binning range? 
absolute_bounds 0

# order parameter for multicanonical. These need to be implemented manually case by case.
# currently implemented: phisq, phi2Sigma2
orderparam phisq

# update weight on the go?
readonly 0

# initial weight update factor (if weight exists, uses the value there instead)
increment 0.5
